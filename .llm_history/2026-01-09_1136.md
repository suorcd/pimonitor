### Changes on 2026-01-09 11:36

Summary of updates:

- Added `flake.nix`:
  - Implemented a Nix flake to provide a reproducible development environment.
  - Configured usage of `rust-overlay` to provide the latest stable Rust toolchain (supporting the 2024 edition specified in Cargo.toml).
  - Defined `nativeBuildInputs` to include `pkg-config`.
  - Defined `buildInputs` to include system libraries required by dependencies:
    - Linux: `alsa-lib` (for `rodio`/`cpal`).
    - macOS: `Security`, `CoreFoundation`, `CoreAudio`, `AudioUnit`.
