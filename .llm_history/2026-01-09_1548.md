### Changes on 2026-01-09 15:48

- Implemented new requirement from AGENTS.md: when polling the Podcast Index `/recent/newfeeds` endpoint, include the `since` URL parameter set to the Unix timestamp for current time minus 86,400 seconds (24 hours).
- Modified function: `pi_get_recent_newfeeds` in `src/main.rs`.
  - Added computation of `since` using `chrono::Utc::now() - chrono::Duration::seconds(86_400)` and passed it via `.query(&[("since", since)])` on the request.
  - Kept existing headers and parsing logic unchanged.

Files affected:
- `src/main.rs`

Rationale:
- Aligns implementation with the newly added guideline in `AGENTS.md` under "Polling for new/recent feeds".
